#pragma once

enum TEXTURE_WRAP
{
	REPEAT,
	MIRRORED_REPEAT,
	CLAMP_TO_EDGE,
	CLAMP_TO_BORDER
};

enum TEXTURE_FILTER
{
	NEAREST,
	LINEAR,
};

enum MIPMAP_FILTER
{
	NEAREST_NEAREST,
	LINEAR_NEAREST,
	NEAREST_LINEAR,
	LINEAR_LINEAR
};